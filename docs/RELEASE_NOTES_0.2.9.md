# WetoCode 0.2.9

## 本次更新

- 修复 WetoCode CLI 首页 Logo 在 ANSI 控制序列、换行和 PTY 分帧下仍显示为 `O` 的问题。
- CLI 首页的大号 Logo 改为 WetoCode 专属的 `W`，旁边的品牌文字仍显示为 `WetoCode`。

## 下载

- `WetoCode-Setup-0.2.9-x64.exe`：Windows x64 安装版。

当前 Windows 安装包尚未进行商业代码签名，系统可能显示“未知发布者”。
