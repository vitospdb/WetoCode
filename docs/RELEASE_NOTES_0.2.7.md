# WetoCode 0.2.7

## 本次更新

- 修复点击 `WetoCode CLI` 后进程立即退出的问题。
- 集成终端恢复为内置执行引擎的真实 `attach --mini` 交互 TUI，不再使用功能受限的简化问答层。
- 保留完整的会话、模型交互、工具调用和快捷键能力，终端标题及用户可见品牌统一显示为 WetoCode。
- 普通项目 Shell 模式和原有权限边界保持不变。

## 下载

- `WetoCode-Setup-0.2.7-x64.exe`：Windows x64 安装版。
- `WetoCode-Portable-0.2.7-x64.zip`：Windows x64 免安装版。
- `SHA256SUMS-0.2.7.txt`：下载文件完整性校验值。

当前 Windows 安装包尚未进行商业代码签名，系统可能显示“未知发布者”。
